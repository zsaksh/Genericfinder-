import { siteMetadata } from "@/lib/seo";

export const metadata = siteMetadata({ title: "How GenericFinder sources data", description: "Our methodology for official drug references, plan metadata, formularies, freshness labels, adapters, and data limitations.", path: "/sources" });
export default function SourcesPage() { return <section className="container-pad py-12"><p className="eyebrow">Trust & transparency</p><h1 className="mt-3 font-serif text-5xl font-semibold">How we source this data</h1><div className="mt-8 grid gap-5 md:grid-cols-2"><Card title="Medicine references" body="The ingestion layer is designed for official sources such as FDA Drugs@FDA, FDA Orange Book files, DailyMed SPL labeling, RxNorm/RxNav identifiers, and licensed drug reference APIs where available." /><Card title="Insurance and formulary data" body="Plan adapters are designed for public Marketplace/CMS plan metadata, insurer-published formularies, machine-readable files, and licensed plan APIs. County, network, and benefit details must be verified." /><Card title="Pharmacy prices" body="Cash prices are modeled as estimates from licensed price feeds or prescription savings partners. The demo cache marks estimates clearly and does not represent live offers." /><Card title="Freshness labels" body="Records store source labels, timestamps, confidence, and estimated/unavailable markers so stale or incomplete fields are visible instead of hidden." /></div><p className="mt-8 rounded-organic bg-paper p-5 text-sm leading-6 text-graphite shadow-card">Demo records in this repository are intentionally limited and labeled as normalized sample data so the product is runnable while real licensed or official feeds are connected through adapters.</p></section>; }
function Card({ title, body }: { title: string; body: string }) { return <div className="rounded-organic bg-paper p-6 shadow-card"><h2 className="font-serif text-3xl font-semibold">{title}</h2><p className="mt-3 leading-7 text-graphite">{body}</p></div>; }
